# Usa l'immagine base di Node.js
FROM node:latest

# Imposta la directory di lavoro nel container
WORKDIR /app

# Copia i file package.json e package-lock.json nel container
COPY package.json package-lock.json ./

# Installa le dipendenze
RUN npm install

# Copia il resto del codice dell'applicazione nel container
COPY . .

# Espone la porta 3000 per il server Angular
EXPOSE 3000

# Comando per avviare il server Angular
CMD ["npm", "start"]
